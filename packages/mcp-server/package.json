{
  "name": "@hacker-agent/mcp-server",
  "version": "1.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun && tsc --emitDeclarationOnly --declaration",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "@hacker-agent/shared": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@hacker-agent/agent": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
